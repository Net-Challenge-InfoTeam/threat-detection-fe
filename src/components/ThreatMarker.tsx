import Safety from "src/types/safety";

interface ThreatMarkerProps {
  safety: Safety;
}

const ThreatMarker = ({ safety }: ThreatMarkerProps) => {
  return; // 여긴 작업해야하는데 API Response를 몰라서 일단 냅뒀어
};

export default ThreatMarker;
